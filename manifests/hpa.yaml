apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: azdevopsapp
  labels:
    app.kubernetes.io/name: azdevopsapp
    app.kubernetes.io/part-of: azdevopsapp-project
    kubernetes.azure.com/generator: devhub
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: azdevopsapp
  minReplicas: 1
  maxReplicas: 5
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 80